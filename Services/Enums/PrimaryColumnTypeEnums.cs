using System;
using System.Collections.Generic;
using System.Text;

namespace Services.Enums
{
    public enum PrimaryColumnTypeEnums
    {
        NONE = -1,
        INT = 0,
        BIGINT = 1,
        UNIQUEIDENTIFIER = 2
    }
}
